{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/worker/Dockerfile",
    "watchPatterns": ["apps/worker/**", "packages/schemas/**"]
  },
  "deploy": {
    "startCommand": "citebell-worker scheduler",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
