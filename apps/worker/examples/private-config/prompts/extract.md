Example prompt for the extract step. The real prompt lives in the private config repo.

Split the article into atomic claims. Return JSON that matches the schema. Never write a number
yourself: refer to each number by its placeholder, and code fills in the verified value.
