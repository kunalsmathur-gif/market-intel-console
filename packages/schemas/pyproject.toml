[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "citebell-schemas"
version = "0.1.0"
description = "Citebell data shapes: defined once in Python, exported as JSON Schema and TypeScript types"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9,<3"]

[tool.setuptools.packages.find]
where = ["src"]
